# Rubric

Placeholder. This file is loaded by `getKnowledge('rubric')`.

It should name what "good" looks like as failure modes and pass criteria: each
failure mode described concretely, and a statement of when a result passes.

Note the scope boundary in `docs/SPEC.md` — prompt-smith is not an evals
platform, and comparison is by eye. A rubric here is a reading aid for the
operator, not a scoring mechanism the app applies on its own.
