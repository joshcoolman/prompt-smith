# Guidance

Placeholder. This file is loaded by `getKnowledge('guidance')` and fed into
prompt assembly.

It should encode what the app should produce and how — composition principles,
style preferences, craft rules for writing personas. Plain markdown, editable
without touching code.

Keep it as short as it can be and still work. Steer with principles and
concrete ranges, not example outputs — examples muzzle the model.
