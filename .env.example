# Any long random string — `openssl rand -hex 32`.
# Changing it invalidates every existing session (the closest thing this design
# has to a global sign-out).
AUTH_SESSION_SECRET=

# Comma-separated allowlist of `email:salt:hash` entries.
# Generate one with: pnpm auth:add-user '<email>' '<password>'
AUTH_USERS=
